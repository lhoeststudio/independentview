const jsdom = require("jsdom");

describe("Element Amounts", () => {

    it("Should Return Correct Number of Items in the List", () => {
        var myElements = 5;

        expect(myElements).toBe(5);
    })
    
});